services:
  semdatasheet:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: semdatasheet
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/src/data
      - ./artifacts:/src/artifacts
    restart: unless-stopped
